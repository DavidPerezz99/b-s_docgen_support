{{!-- This file is a handlebars template used to generate models for params in @Controller annotated lambda handlers --}}
// WARNING: This file was auto-generated with tsoa. Please do not modify it.
import { TsoaRoute } from '@tsoa/runtime';

export const models: TsoaRoute.Models = {
    {{#each models}}
    "{{@key}}": {
        {{#if enums}}
        "dataType": "refEnum",
        "enums": {{{json enums}}},
        {{/if}}
        {{#if properties}}
        "dataType": "refObject",
        "properties": {
            {{#each properties}}
            "{{@key}}": {{{json this}}},
            {{/each}}
        },
        "additionalProperties": {{{json additionalProperties}}},
        {{/if}}
        {{#if type}}
        "dataType": "refAlias",
        "type": {{{json type}}},
        {{/if}}
    },
    {{/each}}
};
